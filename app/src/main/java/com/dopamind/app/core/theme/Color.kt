package com.dopamind.app.core.theme

import androidx.compose.ui.graphics.Color

// Background
val BackgroundPrimary = Color(0xFF0A0A0C)
val BackgroundElevated = Color(0xFF121214)

// Border
val BorderSubtle = Color(0xFF1F1F23)
val BorderHover = Color(0xFF2A2A2E)

// Text
val TextPrimary = Color(0xFFF5F5F7)
val TextSecondary = Color(0xFF8E8E93)
val TextDisabled = Color(0xFF4A4A4E)

// Accent — the single accent color for CTAs, progress rings, and highlights
val Accent = Color(0xFF00D9A3)

// Semantic — reserved for real states only, never decorative
val Warning = Color(0xFFFF9F0A)
val Danger = Color(0xFFFF453A)
