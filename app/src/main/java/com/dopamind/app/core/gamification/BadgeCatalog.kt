package com.dopamind.app.core.gamification

/** Pure Kotlin catalog — display copy (name/emoji) is resolved in the UI layer from strings.xml. */
enum class BadgeId {
    CHECKIN_STREAK_7,
    CHECKIN_STREAK_30,
    TBREAK_HERO_7,
    SMOKE_FREE_3,
    LIVER_FRIEND_7,
    MONK_MODE_7,
    DETOX_MASTER_3,
    BUDGET_BOSS,
}
